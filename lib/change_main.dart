import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class ChangeMainPage extends StatelessWidget {
  const ChangeMainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('not available yet'),
      ),
      body: Center(
        child: Text('change stat not implemented yet'),
      ),
    );
  }
}